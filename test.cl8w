extern fun "memory" "malloc" malloc(x:i32):i32
extern fun "io" "print" print(x:i32)

struct Hoge{
    a:i32,
    b:i32
}

fun main()={
    let x=0;
    while(x!=10){
        print(x);
        x=x+1;
    };
}

